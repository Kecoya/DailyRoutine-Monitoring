# 安装和使用指南

## 系统要求

- **操作系统**: Windows 10/11
- **Python版本**: Python 3.8 或更高
- **磁盘空间**: 至少 100MB 可用空间

## 快速开始

### 方法一：使用批处理文件（推荐）

1. **安装依赖**
   - 双击运行 `安装依赖.bat`
   - 等待所有依赖库安装完成

2. **启动程序**
   - 双击运行 `启动监控.bat`
   - 程序将自动启动监控服务和 Web 界面

3. **访问 Web 界面**
   - 打开浏览器访问: `http://localhost:5000`

### 方法二：使用命令行

1. **克隆仓库并进入项目目录**
   ```bash
   git clone https://github.com/Kecoya/DailyRoutine-Monitoring.git
   cd DailyRoutine-Monitoring
   ```

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

3. **启动程序**
   ```bash
   python main.py
   ```

4. **访问 Web 界面**
   - 打开浏览器访问: `http://localhost:5000`

## 设置开机自启动

### 使用自动配置脚本（推荐）

1. 运行自启动配置工具：
   ```bash
   python setup_autostart.py
   ```

2. 选择选项 `1` 设置开机自启动

3. 重启电脑验证

### 手动设置

1. 按 `Win + R` 打开运行对话框
2. 输入 `shell:startup` 并按回车
3. 在打开的启动文件夹中创建快捷方式：
   - 右键 → 新建 → 快捷方式
   - 目标位置: `pythonw.exe "项目完整路径\silent_launcher.py"`
   - 名称: SystemMonitor
4. 重启电脑验证

## 功能使用

### 1. 实时监控

程序启动后会自动开始监控：
- 鼠标活动（移动、点击）
- 键盘活动（按键次数）
- 窗口切换
- 系统资源使用（CPU、内存）

数据默认每 60 秒自动保存一次，关机时会保存最后的数据。

### 2. 查看统计

访问 Web 界面可以查看：

- **📅 今日概览**: 当天的活动数据、工作达成率和忙碌度曲线
- **📊 本周报表**: 从周一开始的统计分析、趋势图和综合指标
- **📈 本月报表**: 本月的活动日历热力图和统计
- **🔍 自定义分析**: 选择任意时间段进行分析
- **💾 数据导出**: 导出 CSV 或 Excel 格式的报表

### 3. 数据导出

1. 在 Web 界面点击"数据导出"标签
2. 选择开始和结束日期
3. 选择导出格式：
   - **CSV**: 原始数据，适合进一步分析
   - **Excel**: 包含汇总统计，更直观

导出的文件保存在 `data` 目录下。

## 数据说明

### 监控指标

- **鼠标活动**:
  - 移动距离（像素，可换算为米）
  - 点击次数
  - 移动频率

- **键盘活动**:
  - 按键次数
  - 打字速度（每分钟按键数）

- **窗口管理**:
  - 窗口切换次数
  - 活动窗口标题

- **系统状态**:
  - CPU 使用率
  - 内存使用率

### 忙碌指数计算

忙碌指数范围 0-100，综合考虑：

| 指标 | 权重 |
|-----|------|
| 鼠标活动 | 30% |
| 键盘活动 | 30% |
| 窗口切换 | 20% |
| 系统资源 | 20% |

指数说明：
- **0-20**: 空闲 / 午休
- **20-40**: 低强度活动
- **40-60**: 中等强度工作
- **60-80**: 高强度工作
- **80-100**: 极高强度工作

### 综合评估指标

| 指标 | 含义 |
|------|------|
| 工作强度 | 活动时长 × 忙碌指数，综合评估投入程度 |
| 专注度 | 基于窗口切换频率，分数越高越专注 |
| 效率指数 | 单位时间内的操作频率（鼠标 + 键盘） |
| 作息规律性 | 基于每日开机时间标准差，越高越规律 |
| 工作达成率 | 当日实际活动时长 / 标准工时 × 100% |

### 作息分析

程序会自动识别：
- **开机时间**: 每日最早的活动时间
- **关机时间**: 最后的活动时间（凌晨 0-2 点的活动归入前一天）
- **午休时段**: 12:00-14:00 之间的空闲时间
- **长时间空闲**: 连续 10 分钟无活动

## 配置说明

所有配置项在 `config.py` 中，可根据需要调整：

```python
MONITOR_INTERVAL = 60      # 数据保存间隔（秒）
IDLE_THRESHOLD = 600        # 空闲阈值（秒），10分钟
WEB_PORT = 5000             # Web 服务端口
LAB_WORK_HOURS = 8.5        # 标准日工时（小时）
PIXELS_PER_METER = 5200     # 鼠标距离换算系数（像素/米）
DATA_RETENTION_DAYS = 365   # 数据保留天数
```

## 常见问题

### Q1: 程序无法启动

**A**: 检查以下几点：
1. 确认已安装 Python 3.8+
2. 确认已安装所有依赖库（`pip install -r requirements.txt`）
3. 查看 `logs/monitor.log` 日志文件
4. 确保端口 5000 未被占用
5. 如果提示"程序已在运行中"，说明已有实例在后台运行

### Q2: 数据不准确

**A**:
1. 确保监控服务持续运行
2. 程序已内置单实例检测，不会出现多开问题
3. 查看日志确认是否有错误

### Q3: Web 界面无法访问

**A**:
1. 确认程序正在运行
2. 尝试访问 `http://127.0.0.1:5000`
3. 检查防火墙设置
4. 更换端口（修改 `config.py` 中的 `WEB_PORT`）

### Q4: 自启动不生效

**A**:
1. 确认快捷方式在启动文件夹中（`Win+R` → `shell:startup`）
2. 检查快捷方式的目标路径是否正确
3. 查看 `logs/silent_launcher.log` 确认是否启动成功

### Q5: 如何重启服务

**A**: 使用项目提供的 `restart.bat`，它会安全地停止旧进程并启动新进程。

## 卸载程序

1. 移除开机自启动：
   ```bash
   python setup_autostart.py
   ```
   选择选项 `2` 移除自启动

2. 关闭正在运行的服务（Ctrl+C 或运行 `restart.bat` 后关闭窗口）

3. 删除程序文件夹

## 许可证

MIT License - 详见 LICENSE 文件
